"""Bakåtkompatibel fasad för det uppdelade paketet app.backend.mcp.

Implementationen bor i app/backend/mcp/ (protocol, tooling, providers, chat,
service). Importera gärna direkt från paketet i ny kod. Tester som
monkeypatchar interna namn ska patcha implementationsmodulen, inte fasaden.
"""
from .config import settings
from .mcp import chat, protocol, providers, service, tooling

from .mcp.protocol import (
    CLIENT_NAME,
    CLIENT_VERSION,
    DEFAULT_TOKEN_ENV_TEMPLATE,
    MCP_CONTEXT_ITEM_CHAR_LIMIT,
    MCP_CONTEXT_PROMPT_LIMIT,
    MCP_CONTEXT_RESOURCE_LIMIT,
    MCP_CONTEXT_TOTAL_CHAR_LIMIT,
    MCP_LLM_OPENAI_API_KEY_ENV,
    MCP_PROVIDER_LABELS,
    MCP_PROVIDER_ORDER,
    MCP_TOOL_CALLS_PER_STEP_LIMIT,
    MCP_TOOL_CALL_MAX_STEPS,
    MCP_TOOL_RESULT_CHAR_LIMIT,
    McpConfig,
    McpConfigError,
    McpContext,
    McpHttpSession,
    McpProtocolError,
    McpToolCallResult,
    McpToolSummary,
    PROTOCOL_VERSIONS,
    QUESTION_FIELDS,
    READ_ONLY_TOOL_NAME_RE,
    ROOT_DIR,
    SAFE_TOOL_NAME_RE,
    SERVER_NAME,
    TEXTUAL_TOOL_CALL_RE,
    UNSAFE_TOOL_NAME_RE,
    _env_file_value,
    _format_tenant_template,
    _json_payloads_from_sse,
    _load_project_mcp_entry,
    _response_payload,
    _rpc_error_message,
    _strip_env_quotes,
    mcp_authorization_header,
    mcp_config,
    mcp_token_env_var_for_tenant,
    mcp_url_for_tenant,
)
from .mcp.tooling import (
    _clean_gemini_schema,
    _content_item_text,
    _context_label,
    _gemini_function_declaration,
    _input_schema,
    _prompt_arguments,
    _prompt_supports_empty_arguments,
    _question_tool_score,
    _read_only_tool,
    _resource_content_text,
    _resource_description,
    _resource_title,
    _safe_question_tool,
    _schema_default,
    _schema_properties,
    _tool_description,
    _tool_title,
    _trim_context_item,
    arguments_for_question,
    collect_mcp_context,
    extract_prompt_text,
    extract_resource_text,
    extract_tool_answer,
    gemini_function_declarations,
    select_question_tool,
    summarize_prompt,
    summarize_prompts,
    summarize_resource,
    summarize_resources,
    summarize_tool,
    summarize_tools,
)
from .mcp.providers import (
    _csv_values,
    _deepseek_base_url,
    _deepseek_chat_url,
    _default_provider,
    _gemini_base_url,
    _gemini_generate_url,
    _model_options,
    _openai_base_url,
    _provider_configured,
    _provider_missing,
    brain_configured,
    brain_missing,
    brain_model_name,
    brain_provider_name,
    deepseek_configured,
    deepseek_model_name,
    deepseek_reasoning_effort,
    deepseek_thinking_enabled,
    gemini_configured,
    gemini_model_name,
    mcp_brain_options,
    minimax_configured,
    minimax_model_name,
    normalize_thinking_mode,
    nowaste_api_key,
    nowaste_base_url,
    nowaste_configured,
    nowaste_model_name,
    openai_configured,
    openai_model_name,
    provider_model_options,
    provider_thinking_modes,
)
from .mcp.chat import (
    _call_gemini_generate_content,
    _candidate_content,
    _chat_completion_body,
    _chat_completion_for_provider,
    _chat_provider_api_key,
    _chat_provider_url,
    _deepseek_assistant_message,
    _deepseek_body,
    _deepseek_chat_completion,
    _deepseek_message,
    _deepseek_text_has_tool_markup,
    _extract_deepseek_text,
    _extract_deepseek_tool_calls,
    _extract_gemini_function_calls,
    _extract_gemini_text,
    _gemini_body,
    _gemini_generate_content,
    _gemini_system_text,
    _initial_deepseek_messages,
    _initial_gemini_contents,
    _initial_user_text,
    _limit_tool_payload,
    _tool_exhausted_answer,
    openai_tool_declarations,
)
from .mcp.service import (
    ask_chat_provider_with_mcp_context,
    ask_chat_provider_with_mcp_tools,
    ask_deepseek_with_mcp_context,
    ask_deepseek_with_mcp_tools,
    ask_gemini_with_mcp_context,
    ask_gemini_with_mcp_tools,
    ask_mcp,
    list_mcp_tools,
)
