[pytest]
testpaths = tests
markers =
    integration: opt-in tests that talk to external systems
    browser: Playwright-baserade browsertester (auto-marks via chromium_browser-fixturen; pre-push kor -m "not browser", CI ar gate)
